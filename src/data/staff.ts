/**
 * Коды панели персонала для демонстрации без сервера.
 *
 * Честно: в демонстрации проверка идёт на самом устройстве, поэтому эти коды лежат
 * в коде приложения и защитой не являются — они показывают, как устроен вход.
 * Настоящая проверка появляется вместе с сервером: код заведения задаётся в STAFF_PANEL_CODE,
 * личные PIN хранятся только в виде хэша (scrypt) и сверяются на сервере,
 * а подбор останавливают ограничение частоты и блокировка учётной записи.
 */

/** Код заведения — первый шаг: открывает саму панель. */
export const DEMO_PANEL_CODE = '2468';

/** Сотрудники демонстрации: номер и личный PIN. */
export const DEMO_STAFF: { number: string; pin: string; name: string }[] = [
  { number: '01', pin: '1234', name: 'Кухня' },
  { number: '02', pin: '5678', name: 'Кассир' },
];

/** Через сколько минут без действий панель закрывается сама. */
export const STAFF_IDLE_MINUTES = 15;
